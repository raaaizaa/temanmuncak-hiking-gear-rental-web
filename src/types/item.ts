export type itemType = {
    id: number,
    name: string,
    mountain: string,
    image: string,
    quantity: number,
    days: number
    price: number
}

export type partialItemType = {
    id: number;
    name: string;
    image: string;
    price: number;
  };